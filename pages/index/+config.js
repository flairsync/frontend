export default {
  title: "FlairSync",
  // Static marketing page — no user-specific data, so build it once instead
  // of paying SSR cost on every request. https://vike.dev/prerender
  prerender: true,
};
